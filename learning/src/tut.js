let age =24;
let name = age >18 ? 'Adut' :'child'; // ternary operator

const Component =() =>{
    return age > 18 ? <div>Adut</div> :<div>Child</div>
};

//object creation
const person ={
    name:"Person",
    age:20,
    isMarried:false,
};d

const Name = person.name;
const Age = person.age;
const IsMarried =person.isMarried;

